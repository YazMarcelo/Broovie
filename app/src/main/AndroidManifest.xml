<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.broovie.equipe.broovie">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!-- <activity android:name=".activities.Usuario.CadastroUsuarioActivity"></activity> -->

        <activity android:name=".activities.Usuario.CadastroUsuarioActivity" />
        <activity android:name=".activities.TelaPrincipal.TelaPrincipalActivity" />
        <activity
            android:name=".activities.LayoutActivity"
            android:label="@string/title_activity_layout" />
        <activity android:name=".activities.PesquisarActivity" />
        <activity android:name=".activities.FilmeActivity"></activity>
    </application>

</manifest>